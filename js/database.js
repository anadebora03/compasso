/* ============================================================
   COMPASSO · Database — reservado para acesso a dados (Supabase)
   Sprint Supabase 1 — apenas preparação de infraestrutura.
   Nenhuma tabela ou consulta é criada aqui ainda.
   ============================================================ */
import { supabase } from './supabase.js';

export { supabase };
